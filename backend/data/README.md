# Управление контентом

Этот каталог содержит JSON файлы с данными для сайта. Вы можете редактировать их напрямую!

## 📝 articles.json - Статьи

### Формат:
```json
{
  "id": 1,
  "title": "Название статьи",
  "category": "news",
  "content": "Текст статьи...",
  "author": "Имя автора",
  "created_at": "2025-10-26T14:00:00",
  "tags": ["тег1", "тег2"]
}
```

### Доступные категории:
- `news` - Новости
- `patch_notes` - Патч ноты
- `announcements` - Объявления
- `guides` - Гайды
- `events` - События
- `faq` - FAQ

### Как добавить статью:
1. Откройте `articles.json`
2. Добавьте новый объект в массив
3. Укажите уникальный `id` (следующий по порядку)
4. Сохраните файл
5. Обновите страницу в браузере - изменения применятся автоматически!

### Пример:
```json
{
  "id": 6,
  "title": "Турнир выходного дня",
  "category": "events",
  "content": "Присоединяйтесь к турниру! Призы для топ-10 игроков.",
  "author": "Tournament Team",
  "created_at": "2025-10-27T10:00:00",
  "tags": ["tournament", "weekend", "prizes"]
}
```

---

## ⚔️ legends.json - Легенды

### Формат:
```json
{
  "id": 1,
  "name": "Имя легенды",
  "description": "Описание",
  "rarity": "Legendary",
  "stats": {
    "attack": 85,
    "defense": 70,
    "speed": 60
  },
  "abilities": ["Способность 1", "Способность 2"]
}
```

### Редкости (rarity):
- `Common` - Обычная
- `Rare` - Редкая
- `Epic` - Эпическая
- `Legendary` - Легендарная

### Статы (stats):
- `attack` - Атака (0-100)
- `defense` - Защита (0-100)
- `speed` - Скорость (0-100)

---

## 🎴 decks.json - Колоды

### Формат:
```json
{
  "id": 1,
  "name": "Название колоды",
  "description": "Описание стратегии",
  "author": "Имя создателя",
  "legends": [1, 2, 3],
  "likes": 0,
  "views": 0,
  "created_at": "2025-10-26T12:00:00"
}
```

`legends` - массив ID легенд из `legends.json`

---

## 🔄 Автоматическое обновление

Backend автоматически читает эти файлы при каждом запросе, поэтому:
- ✅ Изменения применяются сразу
- ✅ Не нужно перезапускать сервер
- ✅ Данные сохраняются между перезапусками

---

## 🌐 Управление через API

Также можно управлять контентом через API:

### Добавить статью:
```bash
POST http://localhost:8000/api/articles/
```

### Обновить статью:
```bash
PUT http://localhost:8000/api/articles/{id}
```

### Удалить статью:
```bash
DELETE http://localhost:8000/api/articles/{id}
```

Используйте Swagger UI для тестирования: http://localhost:8000/docs

---

## 💡 Советы

1. **Резервные копии**: Делайте копии JSON файлов перед большими изменениями
2. **Валидация**: Проверяйте JSON синтаксис (используйте редактор с подсветкой)
3. **ID**: Всегда используйте уникальные ID для записей
4. **Даты**: Формат даты ISO 8601: `YYYY-MM-DDTHH:MM:SS`
5. **Кодировка**: Файлы должны быть в UTF-8 для корректного отображения кириллицы

---

## 🚀 Будущие улучшения

Для продакшена рекомендуется:
- PostgreSQL база данных
- Админ-панель для управления контентом
- Система авторизации
- Загрузка изображений

